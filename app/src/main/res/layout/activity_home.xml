<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFF8F0">

    <!-- Custom Action Bar -->
    <include
        android:id="@+id/customActionBar"
        layout="@layout/custom_action_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"/>

    <!-- Main Content -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/customActionBar"
        app:layout_constraintBottom_toTopOf="@id/bottomNavigation">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp"
            android:gravity="center">

            <!-- Welcome Image -->
            <ImageView
                android:id="@+id/ivWelcome"
                android:layout_width="389dp"
                android:layout_height="358dp"
                android:layout_marginTop="32dp"
                android:layout_marginBottom="24dp"
                android:src="@drawable/pizzasteve" />

            <!-- Welcome Text -->
            <TextView
                android:id="@+id/tvWelcome"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Welcome to Pizza Steve!"
                android:textSize="28sp"
                android:textStyle="bold"
                android:textColor="#212121"
                android:gravity="center"
                android:layout_marginBottom="16dp"/>

            <!-- Tagline -->
            <TextView
                android:id="@+id/tvTagline"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Delicious pizza delivered to your door! 🚀"
                android:textSize="16sp"
                android:textColor="#757575"                android:gravity="center"
                android:layout_marginBottom="32dp"/>

            <!-- Explore Button -->
            <Button
                android:id="@+id/btnExploreMenu"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Explore Our Menu"
                android:textSize="18sp"
                android:padding="16dp"
                android:textColor="#FFFFFF"
                android:backgroundTint="#E53935"/>

            <!-- Seed Database Button -->
            <Button
                android:id="@+id/btnSeedDatabase"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="🌱 Seed Database"
                android:textSize="16sp"
                android:padding="12dp"
                android:layout_marginTop="8dp"
                android:textColor="#FFFFFF"
                android:backgroundTint="#43A047"/>

            <!-- Logout Button -->
            <Button
                android:id="@+id/btnLogout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Logout"
                android:textSize="16sp"
                android:padding="12dp"
                android:layout_marginTop="8dp"
                android:textColor="#FFFFFF"
                android:backgroundTint="#B71C1C"/>

            <!-- Features -->
            <!-- <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_marginTop="32dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="🍕 Fresh ingredients daily"
                    android:textSize="16sp"
                    android:layout_marginBottom="12dp"
                    android:textColor="#212121"/>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="⚡ Fast delivery"
                    android:textSize="16sp"
                    android:layout_marginBottom="12dp"
                    android:textColor="#212121"/>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="❤️ Made with love"
                    android:textSize="16sp"
                    android:textColor="#212121"/>

            </LinearLayout> -->

        </LinearLayout>

    </ScrollView>

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/bottom_nav_menu"
        android:background="#FFFFFF"
        app:itemIconTint="#E53935"
        app:itemTextColor="#E53935"/>

</androidx.constraintlayout.widget.ConstraintLayout>